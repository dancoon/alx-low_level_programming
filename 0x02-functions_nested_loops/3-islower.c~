#include "main.h"
/**
 *_islower - Checks if a letter is lower case
 *
 *Return: 0 or 1
 */
int _islower(int c)
{
  int c;
  if(c >= 'a' && c <= 'z')
    {
      return (1);
    }
  else
    {
      return (0);
    }
}
